services:
  build:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    environment:
      - NODE_ENV=production
  nginx:
    image: nginx:alpine
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./.output:/app/.output
    ports:
      - "${FRONTEND_PORT}:80"
    depends_on:
      - build
